WUGAS

To install the backend on a Mac, first install pip: 
http://pip.readthedocs.org/en/latest/installing.html

Next, install flask using:
sudo pip install flask

To view the output, run:
python run.py